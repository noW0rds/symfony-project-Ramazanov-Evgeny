{% extends 'base.html.twig' %}

{% block title %}Чек{% endblock %}

{% block body %}
    <h1>Чек</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ check.id }}</td>
            </tr>
            <tr>
                <th>Дата</th>
                <td>{{ check.date ? check.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Магазин</th>
                <td>{{ check.store }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_check_index') }}">Вернуться</a>

    <a href="{{ path('app_check_edit', {'id': check.id}) }}">Редактировать</a>

    {{ include('check/_delete_form.html.twig') }}
{% endblock %}
