<form method="post" action="{{ path('app_check_delete', {'id': check.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить данный чек?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ check.id) }}">
    <button class="btn">Удалить</button>
</form>
