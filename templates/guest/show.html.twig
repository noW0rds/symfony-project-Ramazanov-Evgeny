{% extends 'base.html.twig' %}

{% block title %}Просмотр гостя{% endblock %}

{% block body %}
    <h1>Гость</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ guest.id }}</td>
            </tr>
            <tr>
                <th>Имя</th>
                <td>{{ guest.name }}</td>
            </tr>
            <tr>
                <th>Номер телефона</th>
                <td>{{ guest.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_guest_index') }}">вернуться</a>

    <a href="{{ path('app_guest_edit', {'id': guest.id}) }}">редактировать</a>

    {{ include('guest/_delete_form.html.twig') }}
{% endblock %}
